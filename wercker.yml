#Use OpenJDK base docker
box:
  id: openjdk:8
  ports:
    - 8080
    
#Build app
build:
  steps:
    - install-packages:
        packages: maven
    - script:
        name: maven build
        code: mvn clean assembly:assembly
